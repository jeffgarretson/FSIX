define(['dataservice', 'logger'], function (dataservice, logger) {

    vm = {
        displayName: "Expired Folders",
        activate: activate
    }

    return vm;

    //#region Private functions

    function activate() { }

    //#endregion
});
