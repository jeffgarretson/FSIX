define(['dataservice', 'logger'], function (dataservice, logger) {

    vm = {
        displayName: "New Folder",
        activate: activate
    }

    return vm;

    //#region Private functions

    function activate() {}

    //#endregion
});
